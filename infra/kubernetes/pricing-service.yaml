apiVersion: apps/v1
kind: Deployment
metadata:
  name: pricing-service
  namespace: hotsot
  labels:
    app: hotsot
    service: pricing-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hotsot
      service: pricing-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: hotsot
        service: pricing-service
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: pricing-service
        image: hotsot/pricing-service:latest
        ports:
        - containerPort: 8013
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: hotsot-secrets
              key: database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: hotsot-secrets
              key: redis-url
        - name: KAFKA_BOOTSTRAP
          valueFrom:
            configMapKeyRef:
              name: hotsot-config
              key: kafka-bootstrap
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        livenessProbe:
          httpGet:
            path: /health
            port: 8013
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8013
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: pricing-service
  namespace: hotsot
  labels:
    app: hotsot
    service: pricing-service
spec:
  selector:
    app: hotsot
    service: pricing-service
  ports:
  - port: 8013
    targetPort: 8013
  type: ClusterIP
