apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: hotsot
  labels:
    app: notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: hotsot/notification-service:latest
        ports:
        - containerPort: 8005
        envFrom:
        - configMapRef:
            name: hotsot-config
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8005
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8005
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: hotsot
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
  - port: 8005
    targetPort: 8005
