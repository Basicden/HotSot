apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-service
  namespace: hotsot
  labels:
    app: ml-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ml-service
  template:
    metadata:
      labels:
        app: ml-service
    spec:
      containers:
      - name: ml-service
        image: hotsot/ml-service:latest
        ports:
        - containerPort: 8007
        envFrom:
        - configMapRef:
            name: hotsot-config
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8007
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8007
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: hotsot
spec:
  type: ClusterIP
  selector:
    app: ml-service
  ports:
  - port: 8007
    targetPort: 8007
