apiVersion: apps/v1
kind: Deployment
metadata:
  name: arrival-service
  namespace: hotsot
  labels:
    app: arrival-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: arrival-service
  template:
    metadata:
      labels:
        app: arrival-service
    spec:
      containers:
      - name: arrival-service
        image: hotsot/arrival-service:latest
        ports:
        - containerPort: 8008
        envFrom:
        - configMapRef:
            name: hotsot-config
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8008
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8008
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: arrival-service
  namespace: hotsot
spec:
  type: ClusterIP
  selector:
    app: arrival-service
  ports:
  - port: 8008
    targetPort: 8008
