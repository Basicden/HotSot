apiVersion: apps/v1
kind: Deployment
metadata:
  name: realtime-service
  namespace: hotsot
  labels:
    app: realtime-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: realtime-service
  template:
    metadata:
      labels:
        app: realtime-service
    spec:
      containers:
      - name: realtime-service
        image: hotsot/realtime-service:latest
        ports:
        - containerPort: 8006
        envFrom:
        - configMapRef:
            name: hotsot-config
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8006
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8006
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: realtime-service
  namespace: hotsot
spec:
  type: ClusterIP
  selector:
    app: realtime-service
  ports:
  - port: 8006
    targetPort: 8006
