apiVersion: apps/v1
kind: Deployment
metadata:
  name: shelf-service
  namespace: hotsot
  labels:
    app: shelf-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shelf-service
  template:
    metadata:
      labels:
        app: shelf-service
    spec:
      containers:
      - name: shelf-service
        image: hotsot/shelf-service:latest
        ports:
        - containerPort: 8003
        envFrom:
        - configMapRef:
            name: hotsot-config
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8003
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8003
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: shelf-service
  namespace: hotsot
spec:
  type: ClusterIP
  selector:
    app: shelf-service
  ports:
  - port: 8003
    targetPort: 8003
